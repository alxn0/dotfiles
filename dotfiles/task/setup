#!/bin/bash
set -euo pipefail

# Create symbolic link
echo "creating symbolic link"
ln -sf "$PWD/taskrc" "$HOME/.taskrc"

mkdir -p "$HOME/.task"

# -n so re-running links the dir itself instead of nesting inside an existing link.
ln -sfn "$PWD/hooks" "$HOME/.task/hooks"
